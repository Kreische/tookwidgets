name: tookwidgets
description: Project to manage common widgets for Took App
version: 1.0.1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^7.3.0
  image_picker: ^0.8.4+2
  provider: ^6.0.1
  just_audio: ^0.9.12
  google_maps_flutter: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/Icons/
